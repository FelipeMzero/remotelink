# RemoteLink - Python Dependencies
# Install with: pip install -r requirements.txt

# Core GUI (built-in on most systems)
# tkinter - included with Python

# Screen capture (host side)
mss>=9.0.1

# Image processing
Pillow>=10.0.0

# Input automation (host side)
pyautogui>=0.9.54

# Optional: better performance
numpy>=1.24.0
